node_modules/
dist/
.output/
coverage/
pnpm-lock.yaml
packages/db/migrations/
docs/reference-data/

# Generated by the TanStack Start plugin on every build.
apps/web/src/routeTree.gen.ts

# Generated by `pnpm --filter @klopt/web run openapi`, and compared byte for byte
# by test/openapi.test.ts. Prettier collapses short arrays, which would make the
# checked-in copy differ from the one the API serves — for no gain, since nobody
# hand-edits it.
docs/openapi.json
