node_modules/
dist/
.output/
coverage/
pnpm-lock.yaml
packages/db/migrations/
docs/reference-data/

# Generated by the TanStack Start plugin on every build.
apps/web/src/routeTree.gen.ts

# Both generated by `pnpm --filter @klopt/web run openapi`, and both compared by
# test/openapi.test.ts. Prettier collapses short arrays, which would make the
# checked-in copies differ from what the generator emits — for no gain, since
# nobody hand-edits either of them.
docs/openapi.json
apps/web/src/api/response-schemas.generated.json
